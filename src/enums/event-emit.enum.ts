/**
 * Events emit enum
 * @module enums
 * @enum {string}
 */
export enum EventEmitEnum {
  START = 'start',
  STOP = 'stop',
  ONLINE = 'online',
  MUTE = 'mute',
  OFFER = 'offer',
  ANSWER = 'answer',
  CANDIDATE = 'candidate',
}
