/**
 * Events emit enum
 * @module enums
 * @enum {string}
 */
export enum EventEmitEnum {
  START = 'start',
  ONLINE = 'online',
  MUTE = 'mute',
  OFFER = 'offer',
  ANSWER = 'answer',
  CANDIDATE = 'candidate',
}
